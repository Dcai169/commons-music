extends default

block content
    div(class="container text-center")
        div(class="jumbotron m-3")
            h1 You have been logged out.
            h4 You will be redirected shortly.
            span(onload="signOut()")